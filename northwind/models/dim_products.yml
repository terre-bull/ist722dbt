version: 2


models:
  - name: dim_products
    description: One row per product
    columns:
      - name: productkey
        description: The surrogate key of the product
        tests:
          - not_null
          - unique
      - name: productid
        description: The product key of the source system
        tests:
          - not_null
          - unique
